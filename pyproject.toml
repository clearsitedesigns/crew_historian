[project]
name = "crew_historian"
version = "0.1.0"
description = "crew_historian using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.13"
dependencies = [
    "crewai[tools]>=0.121.1,<1.0.0"
]

[project.scripts]
crew_historian = "crew_historian.main:run"
run_crew = "crew_historian.main:run"
train = "crew_historian.main:train"
replay = "crew_historian.main:replay"
test = "crew_historian.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
